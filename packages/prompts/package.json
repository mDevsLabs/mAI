{
  "name": "mai",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest",
    "test:coverage": "vitest --coverage --silent='passed-only'",
    "test:update": "vitest -u"
  },
  "devDependencies": {
    "@lobechat/types": "workspace:*",
    "tsx": "^4.20.6"
  }
}
