# Version 0.0.10

## Changements apportés :
- **Correction d'erreur de type (document-preview)** 🛠️ : Correction du type de `hitboxRef` dans `components/chat/document-preview.tsx` pour accepter `null`.
- **Correction d'erreur de type (toolbar)** 🛠️ : Ajout de `undefined` comme valeur initiale à `useRef` pour `timeoutRef` dans `components/chat/toolbar.tsx`.
- **Correction d'erreur de type (useOnClickOutside)** 🛠️ : Ajout d'un cast pour `toolbarRef` dans l'appel à `useOnClickOutside` dans `components/chat/toolbar.tsx` pour satisfaire les types attendus.
- **Correction d'erreur d'import (theme-provider)** 🛠️ : Import de `ThemeProviderProps` directement de `next-themes` au lieu de `next-themes/dist/types`.
- **Mise à jour de la version** 🆙 : Passage du projet à la version `0.0.10` dans `package.json`.

## Recommandations futures :
- **Surveiller d'autres erreurs** 🔍 : Continuer à surveiller le build lors des prochaines modifications pour s'assurer qu'aucune autre erreur de type n'apparaisse.
