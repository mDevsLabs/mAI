/**
 * Page Projets — Permet d'organiser les conversations dans des dossiers
 * @version 0.0.2
 */
import { ProjectsPage } from "@/components/chat/projects-page";

export default function Page() {
  return <ProjectsPage />;
}
